@model IEnumerable<Os.Brain.Models.Path>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PathCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PathName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model._Path)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PathCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PathName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item._Path)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.PathId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.PathId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.PathId">Delete</a>
        </td>
    </tr>
}
</table>
</body>
</html>
